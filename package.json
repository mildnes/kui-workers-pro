{
  "name": "kui-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test tests/*.test.js",
    "check": "npm test && node --check 'functions/api/[[path]].js' && python3 -m py_compile static/vps/*.py && bash -n static/vps/*.sh && wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.120.1"
  }
}
